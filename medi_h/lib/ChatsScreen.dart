import 'package:flutter/material.dart';

class ChatsScreen extends StatelessWidget {

  Widget build(BuildContext context){
    return Scaffold(
      body: Center(child: Text("Welcome to chats screen"),),
    );

  }
}